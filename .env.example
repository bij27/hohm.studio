# hohm.studio Environment Configuration

# === REQUIRED: Supabase Database ===
# Get this from: Supabase Dashboard > Project Settings > Database > Connection string (URI)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# === Environment ===
ENVIRONMENT=production

# === Security (Production) ===
# Comma-separated list of allowed origins for CORS
# Example: https://hohm.studio,https://www.hohm.studio
ALLOWED_ORIGINS=https://your-app.onrender.com

# Comma-separated list of allowed hosts (for Trusted Host middleware)
ALLOWED_HOSTS=your-app.onrender.com

# Secret key for session security (generate a random string)
SESSION_SECRET_KEY=your-secure-random-string-here

# === Optional: Rate Limiting ===
RATE_LIMIT_RPM=120
RATE_LIMIT_BURST=30
